//import 'dart:math';

//String generateOTP({required DateTime startTime, required DateTime endTime}) {
  //final random = Random();
  //final otp = List.generate(6, (_) => random.nextInt(10)).join();
  //return '${otp}#${startTime.millisecondsSinceEpoch}#${endTime.millisecondsSinceEpoch}';
//}

//void main() {
  //final startTime = DateTime.now();
  //final endTime = startTime.add(const Duration(minutes: 5));
  //print(generateOTP(startTime: startTime, endTime: endTime));
//}